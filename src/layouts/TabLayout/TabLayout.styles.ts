import { StyleSheet } from 'react-native';

export const styles = StyleSheet.create({
	container: {
		paddingHorizontal: 24
	},
	tabsContainer: {
		backgroundColor: '#fff',
		padding: 10,
		height: '100%'
	}
});
