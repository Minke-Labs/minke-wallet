import { StyleSheet } from 'react-native';
import { screenWidth } from '@styles';

export default StyleSheet.create({
	container: {
		height: 51,
		flexDirection: 'row',
		position: 'absolute',
		bottom: 0,
		width: screenWidth
	}
});
