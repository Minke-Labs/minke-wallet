import { icon, IconType } from './icons';
import { token, TokenType } from './tokens';
import { colors, ColorType } from './colors';

export { icon, token, colors };
export type { IconType, TokenType, ColorType };
