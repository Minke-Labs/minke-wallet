import { StyleSheet } from 'react-native';

export default StyleSheet.create({
	container: {
		height: 50,
		padding: 8,
		borderRadius: 8,
		borderWidth: 1,
		flexDirection: 'row',
		marginBottom: 25
	}
});
