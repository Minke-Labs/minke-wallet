import { View, Text } from 'react-native';
import React from 'react';

const TransactionContactsScreen = () => (
	<View>
		<Text>TransactionContactsScreen</Text>
	</View>
);

export default TransactionContactsScreen;
