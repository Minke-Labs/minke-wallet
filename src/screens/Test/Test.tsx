import React from 'react';
import { BasicLayout } from '@layouts';
import { Token } from '@components';

const Test = () => (
	<BasicLayout>
		<Token name="polydoge" />
	</BasicLayout>
);

export default Test;
