import React from 'react';
import { Text } from '@components';
import { BasicLayout } from '@layouts';

const HelpCentreScreen = () => (
	<BasicLayout>
		<Text>HelpCentreScreen</Text>
	</BasicLayout>
);

export default HelpCentreScreen;
